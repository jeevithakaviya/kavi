#include<stdio.h>
#include<conio.h>
int main()
{
int b;
    printf("\n enter the number");
    scanf("%d",&b);
    while(b%10!=0)
    {
        b++;
    }
printf(" the nearest greater multiple of 10 is \n%d",a);
return 0;
}
