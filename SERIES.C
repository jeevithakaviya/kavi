include <stdio.h>
void main()
{
    int j, n, a = 2, b = 2, next;

    printf("Enter the number of elements: ");
    scanf("%d", &m);

    printf("Fibonaccin of Series: ");

    for (j = 1; j <= n; ++i)
    {
        printf("%d, ", a);
        next = a + b;
        a=b;
        b = next;
    }
    
    }
