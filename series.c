#include <stdio.h>
int main()
{
    int j, m, c = 2, d = 2, next;

    printf("Enter the number of elements: ");
    scanf("%d", &m);

    printf("Fibonaccin of Series: ");

    for (j = 1; j <= n; ++i)
    {
        printf("%d, ", c);
        next = c + d;
        c=d;
        d = next;
    }
    return 0
    }
    
